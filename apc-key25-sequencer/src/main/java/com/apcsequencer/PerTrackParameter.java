package com.apcsequencer;

/**
 * Per-track parameters controlled by Scene Launch hold + knob gestures.
 */
public enum PerTrackParameter {
    PATTERN_ROTATION,
    SWING,
    TRANSPOSE,
    TRACK_PROBABILITY,
    LOOP_MULTIPLIER,
    EUCLIDEAN_DISTRIBUTION,
    PHASE_OFFSET
}
